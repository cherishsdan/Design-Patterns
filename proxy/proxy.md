# proxy

> 代理模式是为对象提供一个代用品或占位符，以便控制对他的访问。代理模式的关键是，当客户不方便直接访问
> 一个对象或者不满足需要的时候，提供一个替身对象来控制对这个对象的访问，客户实际上访问的是替身对象。
> 替身对象对请求做出一些处理后，在把请求转交给本体对象。

- 保护代理
代理可以过滤掉一些请求，用于控制不同权限的对象对目标对象的访问
- 虚拟代理
把一些开销很大的对象，延迟到真正需要它的时候才创建